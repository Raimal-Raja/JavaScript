// methods are  built in function to manulate a string
// 1 str.toUpperCase()
// 2 str.tolowerCase()
// 3 str.trim() removes all whitespaces
// built in mathod doesn't change original string, it return new string each time
// strings are immutables in javasripts


let str1 = "Professor";
let str2 = "Raimal";

let result = str1.concat(str2)
console.log(result)

let x = 10;
x = x++ * ++x;

console.log(x)